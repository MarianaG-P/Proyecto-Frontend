<?php

/*session_start(); // Iniciar la sesión si no se ha iniciado aún

$conteo = obtenerConteoCarrito();
echo json_encode(['success' => true, 'conteo_carrito' => $conteo]);

function obtenerConteoCarrito() {
    if (isset($_SESSION['carrito'])) {
        return count($_SESSION['carrito']);
    }
    return 0;
}
*/
?>